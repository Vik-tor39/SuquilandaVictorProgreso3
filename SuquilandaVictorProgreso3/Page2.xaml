<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SuquilandaVictorProgreso3.Page2"
             BackgroundColor="#34495E"
             Title="Víctor Suquilanda">
    <VerticalStackLayout Padding="20" VerticalOptions="FillAndExpand" Spacing="20">
        <ListView
            x:Name="vsuquilanda_listview"
            ItemsSource="{Binding Aeropuertos}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding DisplayText}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>